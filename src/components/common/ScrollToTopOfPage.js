import { ArrowUpCircleIcon } from "@heroicons/react/20/solid";

export default function ScrollToTop() {
  return (
    <a href="#top" aria-label="Scrolling to top of page">
      <ArrowUpCircleIcon className="icon" />
    </a>
  );
}
